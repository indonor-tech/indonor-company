{
  "buildCommand": "if [ -f admin/package.json ]; then cd admin && npm install && npm run build; else npm install && npm run build; fi",
  "outputDirectory": "admin/dist",
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
